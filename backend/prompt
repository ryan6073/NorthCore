现在开始修第一个问题：调整 Sandbox Workspace 的生命周期和绑定关系。

背景：
当前后端逻辑是：每次创建 run 时，都会单独创建一个 sandbox workspace。也就是 POST /api/v1/conversations/{conversationId}/runs 会为每次 run 创建新的 workspace。

但我们希望改成：
Workspace 不再默认等于一次 run。Workspace 应该作为会话级/可复用的任务工作区存在。用户创建 single 或 group 会话时，可以创建一个新 workspace，或者选择已有 workspace。之后该 conversation 下的所有 run，都默认在这个 workspace 内执行和产出文件。

请只修改 backend，不要修改 frontend。docs 可以先不动，除非必须同步接口说明。

目标行为：
1. 新增或补全 workspace 概念：
   - workspace 是独立实体，可以被 conversation 绑定。
   - 一个 workspace 可以被一个 conversation 使用；后续可以扩展为多个 conversation 复用同一个 workspace。
   - run 创建时不再无条件创建新 workspace，而是优先使用 conversation 已绑定的 workspace。
   - 如果 conversation 没有绑定 workspace，可以在后端兜底创建一个默认 workspace 并绑定到 conversation，避免老接口直接失败。

2. 创建/选择 workspace 的后端能力：
   请检查现有 database.py、runs.py、conversations.py、sandbox_service.py 里是否已有相关字段或函数。
   如果没有，请以最小改动增加：
   - workspace 表或复用 sandboxes 表中适合的字段，具体方案你先判断。
   - conversation 绑定 workspace_id 的字段或关联关系。
   - 创建 workspace 的 service/database 函数。
   - conversation 创建或更新时支持 workspaceId。
   - run 创建时复用 workspace，而不是每次 prepare_workspace(run_id)。

3. run 与 sandbox 的关系调整：
   - run 仍然是一次任务执行记录。
   - workspace 是文件工作区，生命周期长于 run。
   - sandbox/container 可以仍然按 run 启动临时容器，但挂载的目录应该是 conversation 绑定的 workspace_path。
   - 如果多个 run 使用同一个 workspace，需要保证文件读写和 artifact 同步都基于该 workspace。

4. 文件列表接口调整：
   当前右侧产物/文件可能是一股脑平铺返回。
   请让后端的 run files 或 workspace files 返回目录树结构，而不是简单列表。
   例如返回：
   {
     "name": "workspace",
     "type": "directory",
     "children": [
       {
         "name": "src",
         "type": "directory",
         "children": [
           {"name": "App.tsx", "type": "file", "path": "src/App.tsx"}
         ]
       },
       {"name": "README.md", "type": "file", "path": "README.md"}
     ]
   }

5. 兼容性要求：
   - 保留现有 POST /api/v1/conversations/{conversationId}/runs 请求兼容。
   - 老前端即使没有传 workspaceId，也能继续创建 run。
   - 不要破坏现有 artifacts / artifact_versions 逻辑。
   - 不要大规模重构 message_service。
   - 不要改 frontend。

请你先做三件事：
A. 先阅读 backend/app/api/routes/conversations.py、backend/app/api/routes/runs.py、backend/app/services/run_service.py、backend/app/services/sandbox_service.py、backend/app/services/run_scheduler.py、backend/app/database.py，说明当前 run 创建 workspace 的调用链。
B. 给出最小修改方案，明确需要改哪些文件和数据字段。


修完后请输出：
1. 修改了哪些文件。
2. workspace、conversation、run、sandbox 四者现在的关系。
3. 旧接口如何兼容。
4. 如何验证：
   - python -m compileall -q backend/app
   - 创建 conversation
   - 创建/绑定 workspace
   - 在同一 conversation 下连续创建两个 run，确认它们使用同一个 workspace_path
   - 调用 files 接口确认返回目录树结构